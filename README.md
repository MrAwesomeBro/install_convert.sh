# Script to install ImageMagick to Debian

Just clone it, chmod it and run it.

    git clone 
    chmod +x install_Convert.sh
    ./install_Convert.sh

Done. To check the new version run:

    convert --version
